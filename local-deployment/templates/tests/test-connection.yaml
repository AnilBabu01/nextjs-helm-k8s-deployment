apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "local-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "local-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "local-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
